import { DecimalLimiterDirective } from './decimal-limiter.directive';

describe('DecimalLimiterDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalLimiterDirective();
    expect(directive).toBeTruthy();
  });
});
